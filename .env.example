PROVIDER=chatgpt
PORT=3000
API_KEY=
CAPTURE_RAW_REQUESTS=false  # Set true to save every AI request/response exchange
CAPTURE_DIRECTORY=captures  # One complete request/response JSON file per API call
